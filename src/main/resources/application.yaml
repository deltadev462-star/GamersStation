server:
  forward-headers-strategy: native
  servlet:
    context-path: /api/v1

spring:
  application:
    name: gs-marketplace-api
  
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/gamers_station_marketplace}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        jdbc:
          time_zone: UTC
  
  flyway:
    enabled: true
    baseline-on-migrate: true
    validate-on-migrate: true
    locations: classpath:db/migration
  
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 50MB
  
  profiles:
    active: ${SPRING_PROFILE:dev}

# JWT Configuration
jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: ${JWT_ACCESS_EXPIRATION:900000}      # 15 minutes in milliseconds
  refresh-token-expiration: ${JWT_REFRESH_EXPIRATION:604800000}  # 7 days in milliseconds
  issuer: ${JWT_ISSUER:gamers-station}

# OTP Configuration
otp:
  code-length: 4
  ttl-minutes: 5
  resend-cooldown-seconds: 60
  max-attempts-per-day: 5
  sms:
    auth-token: ${SMS_AUTH_TOKEN}
    sender: GStation
    api-url: https://api.taqnyat.sa/v1/messages

# Media Configuration
media:
  storage:
    provider: s3
  max-images-per-ad: 10
  max-size-mb: 10

# AWS Configuration
aws:
  access-key-id: ${AWS_ACCESS_KEY_ID:}
  secret-access-key: ${AWS_SECRET_ACCESS_KEY:}
  s3:
    bucket-name: ${AWS_S3_BUCKET_NAME:gamers-station-marketplace}
    region: ${AWS_S3_REGION:me-south-1}
  cloudfront:
    domain: ${AWS_CLOUDFRONT_DOMAIN:d2s2tw7g1k5ok1.cloudfront.net}

# Messaging Configuration
messaging:
  ably:
    api-key: ${ABLY_API_KEY:}
  websocket:
    allowed-origins: ${WEBSOCKET_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

# Rate Limiting
rate-limit:
  otp:
    per-ip: 10        # per minute
    per-phone: 5      # per minute
  api:
    default: 100      # per minute

# Pagination Defaults
pagination:
  default-page: 0
  default-size: 20
  max-size: 100

# Springdoc OpenAPI
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha

# Email / SMTP Configuration
spring.mail:
  host: ${MAIL_HOST}
  port: ${MAIL_PORT:587}
  username: ${MAIL_USERNAME}
  password: ${MAIL_PASSWORD}
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enabled: true
          required: true
        connectiontimeout: 5000
        timeout: 5000
        writetimeout: 5000

notification:
  mail:
    from: ${MAIL_FROM}
    sender-name: ${MAIL_SENDER_NAME:Gamers Station}
  frontend-url: ${FRONTEND_URL:http://localhost:3000}

# Internationalization
app:
  i18n:
    default-locale: ar
    supported-locales: en,ar
